using RecipesManager.Views;

namespace RecipesManager.ViewModels.Services
{
    /// <summary>
    /// A link between <see cref="MainViewModel"/> and <see cref="MainView"/>
    /// <para>Used in DI</para>
    /// </summary>
    public interface IViewMainViewModel : IViewModel
    {
    }
}
